var info = document.querySelectorAll(".audio_row__duration");
var time=0;
console.log(info);
// if (time != null && time.length != 0) {
// var fullTime = time[0].innerText;
// alert("Продолжительность треков: " + fullTime);
// }

